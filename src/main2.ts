import "@arcgis/map-components/components/arcgis-map";
import "@arcgis/map-components/components/arcgis-zoom";
import {createPokestopsLayer, initMap} from "./mappings.js";

async function main() {
	const {map, view} = initMap();

	// TODO: Add pokestops layer to the map
	// TODO: Style icons for that layer
	// TODO: Display pokemon data in a popup
	// TODO: Add lists of stops to the sidebar with a pokemon data
	// TODO: Create a button to zoom to a chosen stop on a map
}

main();
